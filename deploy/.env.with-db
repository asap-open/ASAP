# ── PostgreSQL (managed by Compose) ───────────────────────────────────────────
POSTGRES_USER=asap
POSTGRES_PASSWORD=changeme
POSTGRES_DB=asap

# ── Server ─────────────────────────────────────────────────────────────────────
# Port exposed on the host for the API server
SERVER_PORT=3000

# Secret used to sign JWTs – use a long, random string in production
JWT_SECRET=change_this_to_a_long_random_secret

# How long JWT tokens remain valid (e.g. 7d, 24h, 60m)
TOKEN_EXP=7d

# Origin that the server will accept requests from (your public client URL)
FRONTEND_DOMAIN=http://localhost

# ── Client ─────────────────────────────────────────────────────────────────────
# Port exposed on the host for the web client
CLIENT_PORT=80

# Public domain or IP where the app will be accessible
DOMAIN_NAME=localhost
